// Configuration pour les différents environnements de build
// Ce fichier est utilisé par Flutter pour définir les variables d'environnement

// Pour le build de production:
// flutter build apk --dart-define=PRODUCTION=true
// flutter build web --dart-define=PRODUCTION=true

// Pour le build de staging:
// flutter build apk --dart-define=STAGING=true
// flutter build web --dart-define=STAGING=true

// Pour le build de développement (par défaut):
// flutter build apk
// flutter build web

// Exemple d'utilisation dans le code:
// const bool isProduction = bool.fromEnvironment('PRODUCTION', defaultValue: false);
// const String apiUrl = String.fromEnvironment('API_URL', defaultValue: 'http://localhost:5001/api');
