{
  "name": "finea-academy-admin",
  "version": "1.0.0",
  "description": "Dashboard administrateur Finéa Académie",
  "private": true,
  "scripts": {
    "build": "./build-admin.sh",
    "start": "cd admin-dashboard && npm start",
    "install": "cd admin-dashboard && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}